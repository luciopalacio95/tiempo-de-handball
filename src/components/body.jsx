import Contador from "./formula/contador";

export default function Body(){
    return (
        <section className="principal sm:mt-18 pt-2 sm:pt-4 items-center justify-items-center min-h-screen font-[family-name:var(--font-geist-sans)]">
            <Contador/>
        </section>
    )
}

